# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# OS noise
.DS_Store
Thumbs.db

# Local environment
.env

# Generated run output: records of prospect and prospect-builder runs, not source.
# Kept out of git so a run can be reviewed and diffed locally without landing in history.
output/
